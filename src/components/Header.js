import React from 'react';

const styles = {
    headerStyle: {
        background: '#5F5B6B',
    }
};

function Header() {
    return (
        <header style={styles.headerStyle} >
            <h1> Zachry Jorgensen </h1>
        </header>
    )
}

export default Header;